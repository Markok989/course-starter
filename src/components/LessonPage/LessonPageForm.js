import React from 'react';
import { connect } from 'react-redux';
import LessonPageRow from './LessonPageRow';

const LessonPageForm = ({ lesson }) => {

    return (
        <div>
            {
                chapters.map((chapter) => {
                    return (
                    )
                })
            }
            <p>{chapters.id}</p>
        </div>
    );
}

export default LessonPageForm;